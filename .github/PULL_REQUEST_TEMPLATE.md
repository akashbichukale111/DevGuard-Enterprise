## What this changes

## Why

## Verification

- [ ] `make verify` passes locally
- [ ] `make verify-replay-ui` passes (if the Command Center or replay bundle changed)
- [ ] No published number was changed by hand — generated documents were re-rendered
- [ ] Evidence directory and proof-pack names are unchanged (they are referenced by code and tests)

## Notes for reviewers
