{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pip install -r requirements.txt && python scripts/build_replay.py && cd frontend && npm ci && NEXT_OUTPUT=export npm run build",
  "outputDirectory": "frontend/out",
  "installCommand": "echo 'deps installed in buildCommand'",
  "framework": null,
  "redirects": [{ "source": "/", "destination": "/command/", "permanent": false }]
}
